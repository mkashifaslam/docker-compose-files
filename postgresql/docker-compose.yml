version: '3.8'

services:

  postgres:
    image: postgres:15.1
    restart: always
    environment:
      POSTGRES_PASSWORD: cassbana123
    ports:
      - 5432:5432
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
 postgres: